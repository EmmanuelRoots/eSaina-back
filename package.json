{
  "name": "esaina_back",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon -e ts --watch './src/**/*.ts' --ignore './src/api/routes/routes.ts' --exec \"npx yarn update-docs && ts-node ./src/index.ts\"",
    "build": "tsc && tsoa spec-and-routes",
    "start": "node dist/server.js",
    "update-docs": "tsoa spec-and-routes",
    "studio": "prisma studio",
    "migrate": "node -e \"const readlineSync = require('readline-sync'); const migrationName = readlineSync.question('Nom de la migration sans espace : '); if (!migrationName) { console.error('Veuillez fournir un nom de migration valide.'); process.exit(1); } require('child_process').execSync('npx prisma migrate dev --name ' + migrationName, { stdio: 'inherit' });\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.16.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^6.16.2",
    "readline-sync": "^1.4.10",
    "swagger-ui-express": "^5.0.1",
    "tsoa": "^6.6.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^24.6.0",
    "@types/swagger-ui-express": "^4.1.8",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
